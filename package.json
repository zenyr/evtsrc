{
  "name": "evtsrc",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "JinhyeokLee <jinhyuklee@mymusictaste.com>",
  "license": "MIT",
  "scripts": {
    "dev": "rm -rf dist || true && swc src -d dist -w -s",
    "build": "rm -rf dist || true && swc src -d dist -s",
    "pack": "spack"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.60",
    "chokidar": "^3.5.3",
    "typescript": "^5.0.4"
  }
}
